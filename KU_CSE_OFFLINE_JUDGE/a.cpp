#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

main()
{
    int a[10];
    vector<int> v(10);
    vector<pair<int,int>> c(10);
    for(int i=0;i<10;i++)
    {
        c[i] = {i*2,i*3};
    }
    return 0;
}
